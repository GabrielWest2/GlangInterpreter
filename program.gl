/*def add(a, b) {
    return a + b;
}

def sub(a, b) {
    return a - b;
}

def mul(a, b) {
    return a * b;
}

def div(a, b) {
    return a / b;
}

var funcs = [add, sub, mul, div];*/

var numMap = Dict().put("a", 89.1).put(12.1, 345.1);

foreach(var pair : numMap){
    print("test: " + pair.k);
}
