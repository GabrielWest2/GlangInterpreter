var black = Color(127, 128, 128);

//ctx.setFont("Arial", PLAIN, 15);

var canvas = Canvas("Tetris", 400, 400);
canvas.show();


def paint(g){
    g.setFont("Arial", BOLD, 15);
    g.setColor(Color(255, 0, 0));
    g.drawOval(0, 0, 90, 190);
    g.drawString("Cool!", 15, 25);

    println("callback called");
}

canvas.setPaintCallback(paint);

while(scanLine() != "close"){

}

canvas.dispose();
while(scanLine() != "end"){

}